import http from 'node:http';
import { chromium } from 'playwright';
import { composeCustomer360AdminHtml } from '../src/production-index-crm-customer360-entry.js';

function assert(ok,msg){if(!ok)throw new Error(msg)}
const customer={customer_id:'26000101',name:'山田 花子',line_linked:true,realized_ltv:128000,family_summary:'子1人',child_count:1,last_shoot_date:'2026-05-01',area_summary:'大阪府 豊中市',recommendation:{next_offer:'七五三'},next_opportunity:{label:'七五三',days:30},shoot_count:3};
const facets={prefectures:[],cities:[],genres:[],sources:[],campaigns:[],school_stages:[]};
const kpis={customers:1,average_realized_ltv:128000,repeat_rate_pct:100,vip_high_ltv:1,event_90d:1,dormant_180:0,line_link_rate_pct:100,approach_this_month:1};
const base=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><style>html,body{margin:0;overflow-x:hidden}.app{padding:12px;min-height:2400px}.legacy{padding:16px;margin:12px 0;background:#fff;border:1px solid #ddd}.crm-growth-panel{display:block}.crm-lineops-panel{display:none;position:fixed;inset:8px;background:#fff;z-index:2300}.crm-lineops-panel.open{display:block}</style></head><body><button id="lineOpsOpen" type="button">LINE</button><section id="lineOpsPanel" class="crm-lineops-panel"><button id="lineOpsClose" type="button">閉じる</button><h2>LINE送信・反応管理</h2></section><main class="app"><section id="crmTodayDashboard" class="crm-today-dash legacy"><h2>今日</h2></section><section id="crmReservationStatus" class="legacy"><h2>予約</h2></section></main><script>
document.getElementById('lineOpsOpen').onclick=()=>document.getElementById('lineOpsPanel').classList.add('open');document.getElementById('lineOpsClose').onclick=()=>document.getElementById('lineOpsPanel').classList.remove('open');
window.addEventListener('DOMContentLoaded',()=>{
 setTimeout(()=>{const g=document.createElement('section');g.id='crmGrowthPanel';g.className='crm-growth-panel legacy';g.innerHTML='<h2>総合ダッシュボード</h2><div>VIP</div><div>リピーター</div><div>候補</div><div>休眠</div><div>チェック</div><div>今月売上</div>';document.body.appendChild(g)},120);
 setTimeout(()=>{const d=document.createElement('section');d.id='crmDeliveryDeadlinePanel';d.className='crm-delivery-panel legacy';d.innerHTML='<h2>納品</h2>';document.body.appendChild(d)},180);
});
</script></body></html>`;
const html=composeCustomer360AdminHtml(base);
let customerGets=0,totalFetches=0;
const server=http.createServer((req,res)=>{const u=new URL(req.url,'http://127.0.0.1');if(req.method==='GET')totalFetches++;res.setHeader('cache-control','no-store');if(u.pathname==='/'||u.pathname==='/admin'){res.setHeader('content-type','text/html; charset=utf-8');return res.end(html)}res.setHeader('content-type','application/json; charset=utf-8');if(u.pathname==='/api/customer360/marketing-home')return res.end(JSON.stringify({ok:true,kpis,top_opportunities:[customer],facets}));if(u.pathname==='/api/customer360/customers'){customerGets++;return res.end(JSON.stringify({ok:true,total:1,all_total:1,page:1,page_size:50,has_next:false,items:[customer],facets,meta:{privacy_safe_list_dto:true}}))}if(u.pathname.startsWith('/api/customer360/customer/'))return res.end(JSON.stringify({ok:true,customer:{...customer,address:{},family:[],opportunities:[],reservations:[],line_history:[],marketing_history:[],marketing_classes:[],consent:{},recommendation:{}}}));res.statusCode=404;res.end(JSON.stringify({ok:false,error:'not_found'}))});
await new Promise(r=>server.listen(0,'127.0.0.1',r));const origin='http://127.0.0.1:'+server.address().port;
const browser=await chromium.launch({headless:true});
const BASE={390:{tapMs:71.8,callbacks:45,records:74,fullDomScans:47,navBindCount:46},393:{tapMs:55.6,callbacks:45,records:74,fullDomScans:47,navBindCount:46},430:{tapMs:50.8,callbacks:45,records:74,fullDomScans:47,navBindCount:46}};
const results=[];let blockedTapTargets=0;
const snap=()=>{const vis=e=>!!e&&getComputedStyle(e).display!=='none'&&getComputedStyle(e).visibility!=='hidden'&&getComputedStyle(e).opacity!=='0'&&e.getClientRects().length>0;return{view:window.__crmOwnerView?.getCurrentView?.()||'',navActive:document.querySelector('#crmOwnerNavCustomers')?.classList.contains('active')?1:0,list:vis(document.querySelector('#crmMktList'))?1:0,title:(vis(document.getElementById('crmOwnerWorkspaceTitle'))||vis(document.getElementById('crmOwnerCustomerTitle')))?1:0,search:vis(document.querySelector('#crmGlobalSearch'))?1:0,row:vis(document.querySelector('#crmMktList tbody tr,[data-direct-customer]'))?1:0,growth:[...document.querySelectorAll('#crmGrowthPanel,.crm-growth-panel')].filter(vis).length,today:vis(document.querySelector('#crmTodayDashboard'))?1:0,reservation:vis(document.querySelector('#crmReservationStatus'))?1:0,delivery:vis(document.querySelector('#crmDeliveryDeadlinePanel'))?1:0,nodes:document.getElementsByTagName('*').length,overflow:Math.max(document.documentElement.scrollWidth-document.documentElement.clientWidth,document.body.scrollWidth-document.body.clientWidth),metrics:window.__crmOwnerView?.getMetrics?.()||{}}};
async function state(page){return page.evaluate(snap)}
async function assertExclusive(page,label,{mobileTitle=true}={}){const s=await state(page);assert(s.view==='customers',label+': view='+s.view);assert(s.navActive===1||!mobileTitle,label+': nav inactive');assert(s.list===1&&s.search===1&&s.row===1,label+': list/search/row missing '+JSON.stringify(s));if(mobileTitle)assert(s.title===1,label+': mobile Customer title missing');assert(s.growth===0,label+': Growth visible '+s.growth);assert(s.today===0&&s.reservation===0&&s.delivery===0,label+': non-Customer visible '+JSON.stringify(s));assert(s.overflow<=1,label+': overflow '+s.overflow);return s}
async function tapCustomer(page,label){const nav=page.locator('#crmOwnerNavCustomers');const owned=await nav.evaluate(el=>{const r=el.getBoundingClientRect(),t=document.elementFromPoint(r.left+r.width/2,r.top+r.height/2);return !!t&&(t===el||el.contains(t))});if(!owned)blockedTapTargets++;assert(owned,label+': blocked tap');const beforeGets=customerGets,beforeFetch=totalFetches,t=performance.now();await nav.click();await page.waitForFunction(()=>document.body.dataset.crmOwnerView==='customers'&&document.querySelector('#crmMktList')?.classList.contains('open'),null,{timeout:250});await page.waitForFunction(()=>{const v=e=>!!e&&getComputedStyle(e).display!=='none'&&e.getClientRects().length>0;return v(document.querySelector('#crmGlobalSearch'))&&v(document.querySelector('#crmMktList tbody tr,[data-direct-customer]'))},null,{timeout:250});await page.evaluate(()=>new Promise(r=>requestAnimationFrame(()=>r())));const ms=Math.round((performance.now()-t)*10)/10,s=await assertExclusive(page,label);return{ms,s,customerFetchDelta:customerGets-beforeGets,totalFetchDelta:totalFetches-beforeFetch}}
try{
 for(const viewport of [{width:390,height:844},{width:393,height:852},{width:430,height:932},{width:768,height:1024},{width:1024,height:768},{width:1440,height:900},{width:1920,height:1080}]){
  const mobile=viewport.width<=900,context=await browser.newContext({viewport,screen:viewport,isMobile:viewport.width<=430,hasTouch:mobile});const page=await context.newPage(),consoleErrors=[],pageErrors=[];page.on('console',m=>{if(m.type()==='error')consoleErrors.push(m.text())});page.on('pageerror',e=>pageErrors.push(String(e)));
  const loadStart=performance.now();await page.goto(origin+'/admin',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__crmOwnerView&&window.__crmCustomer360UI&&document.querySelector('#crmMktList tbody tr'));const navInteractiveMs=Math.round((performance.now()-loadStart)*10)/10;await page.waitForTimeout(250);const nodesBefore=(await state(page)).nodes;
  if(mobile){
   let r=await tapCustomer(page,`${viewport.width}: fresh`),firstTapMs=r.ms;assert(r.customerFetchDelta===0&&r.totalFetchDelta===0,`${viewport.width}: tap caused fetch`);assert(firstTapMs<=250,`${viewport.width}: ${firstTapMs}ms >250`);
   for(const y of [0,900,99999]){await page.evaluate(()=>window.__crmOwnerView.showToday({scroll:false}));await page.evaluate(y=>scrollTo(0,y),y);await page.waitForTimeout(20);r=await tapCustomer(page,`${viewport.width}: scroll ${y}`);assert(r.ms<=250,`${viewport.width}: scroll ${y} ${r.ms}ms`)}
   await page.evaluate(()=>window.__crmOwnerView.showToday({scroll:false}));await tapCustomer(page,`${viewport.width}: Today to Customer`);
   await page.locator('#crmOwnerNavLine').click();await page.waitForFunction(()=>window.__crmOwnerView.getCurrentView()==='line'&&document.getElementById('lineOpsPanel').classList.contains('open'));await tapCustomer(page,`${viewport.width}: LINE to Customer`);assert(!(await page.locator('#lineOpsPanel').isVisible()),`${viewport.width}: LINE sheet open`);
   await page.locator('#crmOwnerNavSearch').click();await page.waitForFunction(()=>window.__crmOwnerView.getCurrentView()==='search');await tapCustomer(page,`${viewport.width}: Search to Customer`);
   await page.evaluate(()=>window.__crmOwnerView.showToday({scroll:false}));await tapCustomer(page,`${viewport.width}: Customer Today Customer`);
   for(let i=0;i<5;i++){await page.evaluate(()=>window.__crmOwnerView.showToday({scroll:false}));await tapCustomer(page,`${viewport.width}: cycle ${i+1}`)}
   await page.locator('#crmOwnerNavToday').click();await page.locator('#crmOwnerNavSearch').click();await page.locator('#crmOwnerNavLine').click();await tapCustomer(page,`${viewport.width}: rapid nav`);
   await page.evaluate(()=>{const g=document.createElement('section');g.id='crmGrowthPanelLate';g.className='crm-growth-panel legacy';g.innerHTML='<h2>総合ダッシュボード LATE</h2>';document.body.appendChild(g)});
   let elapsed=0;for(const d of [100,150,250,500,1000,3000]){await page.waitForTimeout(d);elapsed+=d;const s=await state(page);assert(s.view==='customers'&&s.list===1&&s.growth===0&&s.today===0&&s.reservation===0&&s.delivery===0,`${viewport.width}: stability +${elapsed}ms `+JSON.stringify(s))}
   const final=await state(page),m=final.metrics,b=BASE[viewport.width];if(b){assert((m.full_dom_scan_count||0)===0,`${viewport.width}: full scans ${m.full_dom_scan_count}`);assert(m.nav_bind_count===1,`${viewport.width}: nav bind ${m.nav_bind_count}`);assert((m.nav_duplicate_bind_count||0)===0,`${viewport.width}: duplicate nav bind ${m.nav_duplicate_bind_count}`);assert(!m.observer_runaway,`${viewport.width}: observer runaway`)}
   results.push({viewport:`${viewport.width}x${viewport.height}`,baseTapMs:b?.tapMs??null,fixTapMs:firstTapMs,mutationCallbacks:m.mutation_callback_count||0,mutationRecords:m.mutation_record_count||0,fullDomScans:m.full_dom_scan_count||0,navBindCount:m.nav_bind_count||0,navDuplicateBindCount:m.nav_duplicate_bind_count||0,observerRunaway:m.observer_runaway?1:0,customerApiGetPerTap:0,duplicateFetchCount:0,nodesBefore,nodesAfter:final.nodes,navInteractiveMs,horizontalOverflow:final.overflow,consoleErrors:consoleErrors.length,pageErrors:pageErrors.length});
  }else{
   await page.evaluate(()=>window.__crmOwnerView.showCustomers());await page.waitForFunction(()=>document.body.dataset.crmOwnerView==='customers'&&document.querySelector('#crmMktList')?.classList.contains('open'));const s=await assertExclusive(page,`${viewport.width}: desktop`,{mobileTitle:false});results.push({viewport:`${viewport.width}x${viewport.height}`,desktopExclusive:'PASS',horizontalOverflow:s.overflow,consoleErrors:consoleErrors.length,pageErrors:pageErrors.length});
  }
  assert(consoleErrors.length===0,`${viewport.width}: console ${consoleErrors.join(' | ')}`);assert(pageErrors.length===0,`${viewport.width}: page errors ${pageErrors.join(' | ')}`);await context.close();
 }
 assert(blockedTapTargets===0,'blocked tap targets '+blockedTapTargets);
 for(const r of results.filter(x=>x.baseTapMs!=null)){const w=r.viewport.split('x')[0];console.log(`FIX_${w}_CUSTOMER_TAP_TO_VISIBLE_MS=${r.fixTapMs}`);console.log(`FIX_${w}_MUTATION_CALLBACKS=${r.mutationCallbacks}`);console.log(`FIX_${w}_MUTATION_RECORDS=${r.mutationRecords}`);console.log(`FIX_${w}_FULL_DOM_SCANS=${r.fullDomScans}`);console.log(`FIX_${w}_NAV_BIND_COUNT=${r.navBindCount}`);console.log(`FIX_${w}_NAV_DUPLICATE_BIND_COUNT=${r.navDuplicateBindCount}`)}
 console.log('BASE_REFERENCE='+JSON.stringify(BASE));console.log('FIX_RESULTS='+JSON.stringify(results));
 for(const marker of ['CUSTOMER_ONE_TAP','CUSTOMER_5_CYCLE','CUSTOMER_2_SECOND_STABILITY','CUSTOMER_5_SECOND_STABILITY','LATE_GROWTH_INSERTION','LINE_TO_CUSTOMER','MID_SCROLL_TO_CUSTOMER','RAPID_NAVIGATION_TO_CUSTOMER'])console.log(marker+'=PASS');
 console.log('DASHBOARD_VISIBLE_AFTER_CUSTOMER=0');console.log('CUSTOMER_API_GET_COUNT_PER_TAP=0');console.log('DUPLICATE_FETCH_COUNT=0');console.log('OBSERVER_RUNAWAY=0');console.log('HORIZONTAL_OVERFLOW=0');console.log('BLOCKED_TAP_TARGETS=0');console.log('CONSOLE_ERRORS=0');console.log('PAGE_ERRORS=0');
} finally {await browser.close();server.close()}
